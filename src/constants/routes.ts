export enum ClientRoutes {
  HOME = "/",
  SIGN_IN = "/sign-in",
  SIGN_UP = "/sign-up",
  PROFILE = "/profile",
  SETTINGS = "/settings",
}

export enum AdminRoutes {
  DASHBOARD = "/admin/dashboard",
  USERS = "/admin/users",
  SETTINGS = "/admin/settings",
}
